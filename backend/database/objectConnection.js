const host = "localhost"
const database = "ejemplo_crud"
const user = "root"
const password = "Lucas47"

export const objectConnection = {
    host: host,
    database: database,
    user: user,
    password: password
}